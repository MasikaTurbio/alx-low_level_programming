#!/bin/bash

# Specify the C file name
CFILE="your_c_file.c"

# Specify the output file name
OUTPUT_FILE="c"

# Run the C file through the preprocessor and save the result into the output file
cpp -E "$CFILE" > "$OUTPUT_FILE"

echo "Preprocessing complete. Output saved in $OUTPUT_FILE."
